import React from "react";

interface IFormInputBookingProps {
  id: any;
}
const FormInputBooking = ({ id }: IFormInputBookingProps) => {
  return <div>{id}</div>;
};

export default FormInputBooking;
